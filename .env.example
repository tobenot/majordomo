# 人设层 API 凭证（OpenAI-compatible 或 Anthropic 兼容）。
# 不配也能跑：persona.mode=auto 时会自动降级到离线模板。

# API key
PERSONA_API_KEY=

# API base url，例如 https://api.openai.com/v1 或自建网关
PERSONA_API_BASE=

# 模型名，例如 gpt-4o-mini / glm-4-flash 等便宜模型
PERSONA_MODEL=
